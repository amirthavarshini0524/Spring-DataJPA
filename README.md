# Spring Data JPA Project

This project demonstrates the use of **Spring Data JPA** to build a data access layer for a Spring-based application. It includes JPA entities, repositories, and usage of Spring Data features like CRUD operations, query methods, and specifications.

---

## 🚀 Features

- Use of JPA entities to map to database tables  
- Repository interfaces extending `JpaRepository` / `CrudRepository`  
- Dynamic query generation based on method names  
- Support for **Specifications** (criteria API) for complex queries :contentReference[oaicite:0]{index=0}  
- Paging and sorting support  
- Auditing (e.g. created/modified timestamps) :contentReference[oaicite:1]{index=1}  
- Transaction management with Spring & JPA  

---

## 🛠️ Tech Stack

| Technology Layer | Used Libraries / Frameworks |
|---|---|
| Application Framework | Spring Boot / Spring Context |
| Persistence | Spring Data JPA, JPA / Hibernate |
| Database | (Configurable – e.g. H2, MySQL, PostgreSQL) |
| Build Tool | Maven or Gradle |
| Java Version | Java 11 / Java 17 (or as per project) |

---
